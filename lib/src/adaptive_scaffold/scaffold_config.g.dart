// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'scaffold_config.dart';

// **************************************************************************
// CopyWithGenerator
// **************************************************************************

abstract class _$AdaptiveScaffoldConfigCWProxy {
  AdaptiveScaffoldConfig navigationRailConfig(
      NavigationRailConfig navigationRailConfig);

  AdaptiveScaffoldConfig appBar(AdaptiveAppBar? appBar);

  AdaptiveScaffoldConfig bodyConfig(BodyConfig bodyConfig);

  AdaptiveScaffoldConfig breakpointConfig(BreakpointConfig breakpointConfig);

  AdaptiveScaffoldConfig drawerConfig(AdaptiveDrawerConfig drawerConfig);

  AdaptiveScaffoldConfig scrollbarConfig(
      AdaptiveScrollbarConfig scrollbarConfig);

  AdaptiveScaffoldConfig useInternalAnimations(bool useInternalAnimations);

  AdaptiveScaffoldConfig useSalomonBar(bool useSalomonBar);

  AdaptiveScaffoldConfig iconSize(double? iconSize);

  AdaptiveScaffoldConfig floatingActionButton(Widget? floatingActionButton);

  AdaptiveScaffoldConfig floatingActionButtonLocation(
      FloatingActionButtonLocation? floatingActionButtonLocation);

  AdaptiveScaffoldConfig floatingActionButtonAnimator(
      FloatingActionButtonAnimator? floatingActionButtonAnimator);

  AdaptiveScaffoldConfig persistentFooterButtons(
      List<Widget>? persistentFooterButtons);

  AdaptiveScaffoldConfig persistentFooterAlignment(
      AlignmentDirectional persistentFooterAlignment);

  AdaptiveScaffoldConfig bottomSheet(Widget? bottomSheet);

  AdaptiveScaffoldConfig backgroundColor(Color? backgroundColor);

  AdaptiveScaffoldConfig resizeToAvoidBottomInset(
      bool? resizeToAvoidBottomInset);

  AdaptiveScaffoldConfig primary(bool primary);

  AdaptiveScaffoldConfig extendBody(bool extendBody);

  AdaptiveScaffoldConfig extendBodyBehindAppBar(bool extendBodyBehindAppBar);

  AdaptiveScaffoldConfig restorationId(String? restorationId);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveScaffoldConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScaffoldConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveScaffoldConfig call({
    NavigationRailConfig? navigationRailConfig,
    AdaptiveAppBar? appBar,
    BodyConfig? bodyConfig,
    BreakpointConfig? breakpointConfig,
    AdaptiveDrawerConfig? drawerConfig,
    AdaptiveScrollbarConfig? scrollbarConfig,
    bool? useInternalAnimations,
    bool? useSalomonBar,
    double? iconSize,
    Widget? floatingActionButton,
    FloatingActionButtonLocation? floatingActionButtonLocation,
    FloatingActionButtonAnimator? floatingActionButtonAnimator,
    List<Widget>? persistentFooterButtons,
    AlignmentDirectional? persistentFooterAlignment,
    Widget? bottomSheet,
    Color? backgroundColor,
    bool? resizeToAvoidBottomInset,
    bool? primary,
    bool? extendBody,
    bool? extendBodyBehindAppBar,
    String? restorationId,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfAdaptiveScaffoldConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfAdaptiveScaffoldConfig.copyWith.fieldName(...)`
class _$AdaptiveScaffoldConfigCWProxyImpl
    implements _$AdaptiveScaffoldConfigCWProxy {
  const _$AdaptiveScaffoldConfigCWProxyImpl(this._value);

  final AdaptiveScaffoldConfig _value;

  @override
  AdaptiveScaffoldConfig navigationRailConfig(
          NavigationRailConfig navigationRailConfig) =>
      this(navigationRailConfig: navigationRailConfig);

  @override
  AdaptiveScaffoldConfig appBar(AdaptiveAppBar? appBar) => this(appBar: appBar);

  @override
  AdaptiveScaffoldConfig bodyConfig(BodyConfig bodyConfig) =>
      this(bodyConfig: bodyConfig);

  @override
  AdaptiveScaffoldConfig breakpointConfig(BreakpointConfig breakpointConfig) =>
      this(breakpointConfig: breakpointConfig);

  @override
  AdaptiveScaffoldConfig drawerConfig(AdaptiveDrawerConfig drawerConfig) =>
      this(drawerConfig: drawerConfig);

  @override
  AdaptiveScaffoldConfig scrollbarConfig(
          AdaptiveScrollbarConfig scrollbarConfig) =>
      this(scrollbarConfig: scrollbarConfig);

  @override
  AdaptiveScaffoldConfig useInternalAnimations(bool useInternalAnimations) =>
      this(useInternalAnimations: useInternalAnimations);

  @override
  AdaptiveScaffoldConfig useSalomonBar(bool useSalomonBar) =>
      this(useSalomonBar: useSalomonBar);

  @override
  AdaptiveScaffoldConfig iconSize(double? iconSize) => this(iconSize: iconSize);

  @override
  AdaptiveScaffoldConfig floatingActionButton(Widget? floatingActionButton) =>
      this(floatingActionButton: floatingActionButton);

  @override
  AdaptiveScaffoldConfig floatingActionButtonLocation(
          FloatingActionButtonLocation? floatingActionButtonLocation) =>
      this(floatingActionButtonLocation: floatingActionButtonLocation);

  @override
  AdaptiveScaffoldConfig floatingActionButtonAnimator(
          FloatingActionButtonAnimator? floatingActionButtonAnimator) =>
      this(floatingActionButtonAnimator: floatingActionButtonAnimator);

  @override
  AdaptiveScaffoldConfig persistentFooterButtons(
          List<Widget>? persistentFooterButtons) =>
      this(persistentFooterButtons: persistentFooterButtons);

  @override
  AdaptiveScaffoldConfig persistentFooterAlignment(
          AlignmentDirectional persistentFooterAlignment) =>
      this(persistentFooterAlignment: persistentFooterAlignment);

  @override
  AdaptiveScaffoldConfig bottomSheet(Widget? bottomSheet) =>
      this(bottomSheet: bottomSheet);

  @override
  AdaptiveScaffoldConfig backgroundColor(Color? backgroundColor) =>
      this(backgroundColor: backgroundColor);

  @override
  AdaptiveScaffoldConfig resizeToAvoidBottomInset(
          bool? resizeToAvoidBottomInset) =>
      this(resizeToAvoidBottomInset: resizeToAvoidBottomInset);

  @override
  AdaptiveScaffoldConfig primary(bool primary) => this(primary: primary);

  @override
  AdaptiveScaffoldConfig extendBody(bool extendBody) =>
      this(extendBody: extendBody);

  @override
  AdaptiveScaffoldConfig extendBodyBehindAppBar(bool extendBodyBehindAppBar) =>
      this(extendBodyBehindAppBar: extendBodyBehindAppBar);

  @override
  AdaptiveScaffoldConfig restorationId(String? restorationId) =>
      this(restorationId: restorationId);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveScaffoldConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScaffoldConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveScaffoldConfig call({
    Object? navigationRailConfig = const $CopyWithPlaceholder(),
    Object? appBar = const $CopyWithPlaceholder(),
    Object? bodyConfig = const $CopyWithPlaceholder(),
    Object? breakpointConfig = const $CopyWithPlaceholder(),
    Object? drawerConfig = const $CopyWithPlaceholder(),
    Object? scrollbarConfig = const $CopyWithPlaceholder(),
    Object? useInternalAnimations = const $CopyWithPlaceholder(),
    Object? useSalomonBar = const $CopyWithPlaceholder(),
    Object? iconSize = const $CopyWithPlaceholder(),
    Object? floatingActionButton = const $CopyWithPlaceholder(),
    Object? floatingActionButtonLocation = const $CopyWithPlaceholder(),
    Object? floatingActionButtonAnimator = const $CopyWithPlaceholder(),
    Object? persistentFooterButtons = const $CopyWithPlaceholder(),
    Object? persistentFooterAlignment = const $CopyWithPlaceholder(),
    Object? bottomSheet = const $CopyWithPlaceholder(),
    Object? backgroundColor = const $CopyWithPlaceholder(),
    Object? resizeToAvoidBottomInset = const $CopyWithPlaceholder(),
    Object? primary = const $CopyWithPlaceholder(),
    Object? extendBody = const $CopyWithPlaceholder(),
    Object? extendBodyBehindAppBar = const $CopyWithPlaceholder(),
    Object? restorationId = const $CopyWithPlaceholder(),
  }) {
    return AdaptiveScaffoldConfig(
      navigationRailConfig:
          navigationRailConfig == const $CopyWithPlaceholder() ||
                  navigationRailConfig == null
              ? _value.navigationRailConfig
              // ignore: cast_nullable_to_non_nullable
              : navigationRailConfig as NavigationRailConfig,
      appBar: appBar == const $CopyWithPlaceholder()
          ? _value.appBar
          // ignore: cast_nullable_to_non_nullable
          : appBar as AdaptiveAppBar?,
      bodyConfig:
          bodyConfig == const $CopyWithPlaceholder() || bodyConfig == null
              ? _value.bodyConfig
              // ignore: cast_nullable_to_non_nullable
              : bodyConfig as BodyConfig,
      breakpointConfig: breakpointConfig == const $CopyWithPlaceholder() ||
              breakpointConfig == null
          ? _value.breakpointConfig
          // ignore: cast_nullable_to_non_nullable
          : breakpointConfig as BreakpointConfig,
      drawerConfig:
          drawerConfig == const $CopyWithPlaceholder() || drawerConfig == null
              ? _value.drawerConfig
              // ignore: cast_nullable_to_non_nullable
              : drawerConfig as AdaptiveDrawerConfig,
      scrollbarConfig: scrollbarConfig == const $CopyWithPlaceholder() ||
              scrollbarConfig == null
          ? _value.scrollbarConfig
          // ignore: cast_nullable_to_non_nullable
          : scrollbarConfig as AdaptiveScrollbarConfig,
      useInternalAnimations:
          useInternalAnimations == const $CopyWithPlaceholder() ||
                  useInternalAnimations == null
              ? _value.useInternalAnimations
              // ignore: cast_nullable_to_non_nullable
              : useInternalAnimations as bool,
      useSalomonBar:
          useSalomonBar == const $CopyWithPlaceholder() || useSalomonBar == null
              ? _value.useSalomonBar
              // ignore: cast_nullable_to_non_nullable
              : useSalomonBar as bool,
      iconSize: iconSize == const $CopyWithPlaceholder()
          ? _value.iconSize
          // ignore: cast_nullable_to_non_nullable
          : iconSize as double?,
      floatingActionButton: floatingActionButton == const $CopyWithPlaceholder()
          ? _value.floatingActionButton
          // ignore: cast_nullable_to_non_nullable
          : floatingActionButton as Widget?,
      floatingActionButtonLocation:
          floatingActionButtonLocation == const $CopyWithPlaceholder()
              ? _value.floatingActionButtonLocation
              // ignore: cast_nullable_to_non_nullable
              : floatingActionButtonLocation as FloatingActionButtonLocation?,
      floatingActionButtonAnimator:
          floatingActionButtonAnimator == const $CopyWithPlaceholder()
              ? _value.floatingActionButtonAnimator
              // ignore: cast_nullable_to_non_nullable
              : floatingActionButtonAnimator as FloatingActionButtonAnimator?,
      persistentFooterButtons:
          persistentFooterButtons == const $CopyWithPlaceholder()
              ? _value.persistentFooterButtons
              // ignore: cast_nullable_to_non_nullable
              : persistentFooterButtons as List<Widget>?,
      persistentFooterAlignment:
          persistentFooterAlignment == const $CopyWithPlaceholder() ||
                  persistentFooterAlignment == null
              ? _value.persistentFooterAlignment
              // ignore: cast_nullable_to_non_nullable
              : persistentFooterAlignment as AlignmentDirectional,
      bottomSheet: bottomSheet == const $CopyWithPlaceholder()
          ? _value.bottomSheet
          // ignore: cast_nullable_to_non_nullable
          : bottomSheet as Widget?,
      backgroundColor: backgroundColor == const $CopyWithPlaceholder()
          ? _value.backgroundColor
          // ignore: cast_nullable_to_non_nullable
          : backgroundColor as Color?,
      resizeToAvoidBottomInset:
          resizeToAvoidBottomInset == const $CopyWithPlaceholder()
              ? _value.resizeToAvoidBottomInset
              // ignore: cast_nullable_to_non_nullable
              : resizeToAvoidBottomInset as bool?,
      primary: primary == const $CopyWithPlaceholder() || primary == null
          ? _value.primary
          // ignore: cast_nullable_to_non_nullable
          : primary as bool,
      extendBody:
          extendBody == const $CopyWithPlaceholder() || extendBody == null
              ? _value.extendBody
              // ignore: cast_nullable_to_non_nullable
              : extendBody as bool,
      extendBodyBehindAppBar:
          extendBodyBehindAppBar == const $CopyWithPlaceholder() ||
                  extendBodyBehindAppBar == null
              ? _value.extendBodyBehindAppBar
              // ignore: cast_nullable_to_non_nullable
              : extendBodyBehindAppBar as bool,
      restorationId: restorationId == const $CopyWithPlaceholder()
          ? _value.restorationId
          // ignore: cast_nullable_to_non_nullable
          : restorationId as String?,
    );
  }
}

extension $AdaptiveScaffoldConfigCopyWith on AdaptiveScaffoldConfig {
  /// Returns a callable class that can be used as follows: `instanceOfAdaptiveScaffoldConfig.copyWith(...)` or like so:`instanceOfAdaptiveScaffoldConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$AdaptiveScaffoldConfigCWProxy get copyWith =>
      _$AdaptiveScaffoldConfigCWProxyImpl(this);

  /// Copies the object with the specific fields set to `null`. If you pass `false` as a parameter, nothing will be done and it will be ignored. Don't do it. Prefer `copyWith(field: null)` or `AdaptiveScaffoldConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScaffoldConfig(...).copyWithNull(firstField: true, secondField: true)
  /// ````
  AdaptiveScaffoldConfig copyWithNull({
    bool appBar = false,
    bool iconSize = false,
    bool floatingActionButton = false,
    bool floatingActionButtonLocation = false,
    bool floatingActionButtonAnimator = false,
    bool persistentFooterButtons = false,
    bool bottomSheet = false,
    bool backgroundColor = false,
    bool resizeToAvoidBottomInset = false,
    bool restorationId = false,
  }) {
    return AdaptiveScaffoldConfig(
      navigationRailConfig: navigationRailConfig,
      appBar: appBar == true ? null : this.appBar,
      bodyConfig: bodyConfig,
      breakpointConfig: breakpointConfig,
      drawerConfig: drawerConfig,
      scrollbarConfig: scrollbarConfig,
      useInternalAnimations: useInternalAnimations,
      useSalomonBar: useSalomonBar,
      iconSize: iconSize == true ? null : this.iconSize,
      floatingActionButton:
          floatingActionButton == true ? null : this.floatingActionButton,
      floatingActionButtonLocation: floatingActionButtonLocation == true
          ? null
          : this.floatingActionButtonLocation,
      floatingActionButtonAnimator: floatingActionButtonAnimator == true
          ? null
          : this.floatingActionButtonAnimator,
      persistentFooterButtons:
          persistentFooterButtons == true ? null : this.persistentFooterButtons,
      persistentFooterAlignment: persistentFooterAlignment,
      bottomSheet: bottomSheet == true ? null : this.bottomSheet,
      backgroundColor: backgroundColor == true ? null : this.backgroundColor,
      resizeToAvoidBottomInset: resizeToAvoidBottomInset == true
          ? null
          : this.resizeToAvoidBottomInset,
      primary: primary,
      extendBody: extendBody,
      extendBodyBehindAppBar: extendBodyBehindAppBar,
      restorationId: restorationId == true ? null : this.restorationId,
    );
  }
}

abstract class _$NavigationRailConfigCWProxy {
  NavigationRailConfig destinations(List<NavigationDestination> destinations);

  NavigationRailConfig selectedIndex(int? selectedIndex);

  NavigationRailConfig width(double? width);

  NavigationRailConfig extendedWidth(double? extendedWidth);

  NavigationRailConfig leading(Widget? leading);

  NavigationRailConfig leadingExtended(Widget? leadingExtended);

  NavigationRailConfig trailing(Widget? trailing);

  NavigationRailConfig key(Key? key);

  NavigationRailConfig backgroundColor(Color? backgroundColor);

  NavigationRailConfig onDestinationSelected(
      void Function(int, NavigationDestination, NavigationRailConfig)?
          onDestinationSelected);

  NavigationRailConfig elevation(double? elevation);

  NavigationRailConfig groupAlignment(double? groupAlignment);

  NavigationRailConfig labelType(NavigationRailLabelType? labelType);

  NavigationRailConfig unselectedLabelTextStyle(
      ResizableTextStyle? unselectedLabelTextStyle);

  NavigationRailConfig selectedLabelTextStyle(
      ResizableTextStyle? selectedLabelTextStyle);

  NavigationRailConfig unselectedIconTheme(IconThemeData? unselectedIconTheme);

  NavigationRailConfig selectedIconTheme(IconThemeData? selectedIconTheme);

  NavigationRailConfig useIndicator(bool? useIndicator);

  NavigationRailConfig indicatorColor(Color? indicatorColor);

  NavigationRailConfig indicatorShape(ShapeBorder? indicatorShape);

  NavigationRailConfig padding(EdgeInsetsGeometry padding);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `NavigationRailConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// NavigationRailConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  NavigationRailConfig call({
    List<NavigationDestination>? destinations,
    int? selectedIndex,
    double? width,
    double? extendedWidth,
    Widget? leading,
    Widget? leadingExtended,
    Widget? trailing,
    Key? key,
    Color? backgroundColor,
    void Function(int, NavigationDestination, NavigationRailConfig)?
        onDestinationSelected,
    double? elevation,
    double? groupAlignment,
    NavigationRailLabelType? labelType,
    ResizableTextStyle? unselectedLabelTextStyle,
    ResizableTextStyle? selectedLabelTextStyle,
    IconThemeData? unselectedIconTheme,
    IconThemeData? selectedIconTheme,
    bool? useIndicator,
    Color? indicatorColor,
    ShapeBorder? indicatorShape,
    EdgeInsetsGeometry? padding,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfNavigationRailConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfNavigationRailConfig.copyWith.fieldName(...)`
class _$NavigationRailConfigCWProxyImpl
    implements _$NavigationRailConfigCWProxy {
  const _$NavigationRailConfigCWProxyImpl(this._value);

  final NavigationRailConfig _value;

  @override
  NavigationRailConfig destinations(List<NavigationDestination> destinations) =>
      this(destinations: destinations);

  @override
  NavigationRailConfig selectedIndex(int? selectedIndex) =>
      this(selectedIndex: selectedIndex);

  @override
  NavigationRailConfig width(double? width) => this(width: width);

  @override
  NavigationRailConfig extendedWidth(double? extendedWidth) =>
      this(extendedWidth: extendedWidth);

  @override
  NavigationRailConfig leading(Widget? leading) => this(leading: leading);

  @override
  NavigationRailConfig leadingExtended(Widget? leadingExtended) =>
      this(leadingExtended: leadingExtended);

  @override
  NavigationRailConfig trailing(Widget? trailing) => this(trailing: trailing);

  @override
  NavigationRailConfig key(Key? key) => this(key: key);

  @override
  NavigationRailConfig backgroundColor(Color? backgroundColor) =>
      this(backgroundColor: backgroundColor);

  @override
  NavigationRailConfig onDestinationSelected(
          void Function(int, NavigationDestination, NavigationRailConfig)?
              onDestinationSelected) =>
      this(onDestinationSelected: onDestinationSelected);

  @override
  NavigationRailConfig elevation(double? elevation) =>
      this(elevation: elevation);

  @override
  NavigationRailConfig groupAlignment(double? groupAlignment) =>
      this(groupAlignment: groupAlignment);

  @override
  NavigationRailConfig labelType(NavigationRailLabelType? labelType) =>
      this(labelType: labelType);

  @override
  NavigationRailConfig unselectedLabelTextStyle(
          ResizableTextStyle? unselectedLabelTextStyle) =>
      this(unselectedLabelTextStyle: unselectedLabelTextStyle);

  @override
  NavigationRailConfig selectedLabelTextStyle(
          ResizableTextStyle? selectedLabelTextStyle) =>
      this(selectedLabelTextStyle: selectedLabelTextStyle);

  @override
  NavigationRailConfig unselectedIconTheme(
          IconThemeData? unselectedIconTheme) =>
      this(unselectedIconTheme: unselectedIconTheme);

  @override
  NavigationRailConfig selectedIconTheme(IconThemeData? selectedIconTheme) =>
      this(selectedIconTheme: selectedIconTheme);

  @override
  NavigationRailConfig useIndicator(bool? useIndicator) =>
      this(useIndicator: useIndicator);

  @override
  NavigationRailConfig indicatorColor(Color? indicatorColor) =>
      this(indicatorColor: indicatorColor);

  @override
  NavigationRailConfig indicatorShape(ShapeBorder? indicatorShape) =>
      this(indicatorShape: indicatorShape);

  @override
  NavigationRailConfig padding(EdgeInsetsGeometry padding) =>
      this(padding: padding);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `NavigationRailConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// NavigationRailConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  NavigationRailConfig call({
    Object? destinations = const $CopyWithPlaceholder(),
    Object? selectedIndex = const $CopyWithPlaceholder(),
    Object? width = const $CopyWithPlaceholder(),
    Object? extendedWidth = const $CopyWithPlaceholder(),
    Object? leading = const $CopyWithPlaceholder(),
    Object? leadingExtended = const $CopyWithPlaceholder(),
    Object? trailing = const $CopyWithPlaceholder(),
    Object? key = const $CopyWithPlaceholder(),
    Object? backgroundColor = const $CopyWithPlaceholder(),
    Object? onDestinationSelected = const $CopyWithPlaceholder(),
    Object? elevation = const $CopyWithPlaceholder(),
    Object? groupAlignment = const $CopyWithPlaceholder(),
    Object? labelType = const $CopyWithPlaceholder(),
    Object? unselectedLabelTextStyle = const $CopyWithPlaceholder(),
    Object? selectedLabelTextStyle = const $CopyWithPlaceholder(),
    Object? unselectedIconTheme = const $CopyWithPlaceholder(),
    Object? selectedIconTheme = const $CopyWithPlaceholder(),
    Object? useIndicator = const $CopyWithPlaceholder(),
    Object? indicatorColor = const $CopyWithPlaceholder(),
    Object? indicatorShape = const $CopyWithPlaceholder(),
    Object? padding = const $CopyWithPlaceholder(),
  }) {
    return NavigationRailConfig(
      destinations:
          destinations == const $CopyWithPlaceholder() || destinations == null
              ? _value.destinations
              // ignore: cast_nullable_to_non_nullable
              : destinations as List<NavigationDestination>,
      selectedIndex: selectedIndex == const $CopyWithPlaceholder()
          ? _value.selectedIndex
          // ignore: cast_nullable_to_non_nullable
          : selectedIndex as int?,
      width: width == const $CopyWithPlaceholder()
          ? _value.width
          // ignore: cast_nullable_to_non_nullable
          : width as double?,
      extendedWidth: extendedWidth == const $CopyWithPlaceholder()
          ? _value.extendedWidth
          // ignore: cast_nullable_to_non_nullable
          : extendedWidth as double?,
      leading: leading == const $CopyWithPlaceholder()
          ? _value.leading
          // ignore: cast_nullable_to_non_nullable
          : leading as Widget?,
      leadingExtended: leadingExtended == const $CopyWithPlaceholder()
          ? _value.leadingExtended
          // ignore: cast_nullable_to_non_nullable
          : leadingExtended as Widget?,
      trailing: trailing == const $CopyWithPlaceholder()
          ? _value.trailing
          // ignore: cast_nullable_to_non_nullable
          : trailing as Widget?,
      key: key == const $CopyWithPlaceholder()
          ? _value.key
          // ignore: cast_nullable_to_non_nullable
          : key as Key?,
      backgroundColor: backgroundColor == const $CopyWithPlaceholder()
          ? _value.backgroundColor
          // ignore: cast_nullable_to_non_nullable
          : backgroundColor as Color?,
      onDestinationSelected:
          onDestinationSelected == const $CopyWithPlaceholder()
              ? _value.onDestinationSelected
              // ignore: cast_nullable_to_non_nullable
              : onDestinationSelected as void Function(
                  int, NavigationDestination, NavigationRailConfig)?,
      elevation: elevation == const $CopyWithPlaceholder()
          ? _value.elevation
          // ignore: cast_nullable_to_non_nullable
          : elevation as double?,
      groupAlignment: groupAlignment == const $CopyWithPlaceholder()
          ? _value.groupAlignment
          // ignore: cast_nullable_to_non_nullable
          : groupAlignment as double?,
      labelType: labelType == const $CopyWithPlaceholder()
          ? _value.labelType
          // ignore: cast_nullable_to_non_nullable
          : labelType as NavigationRailLabelType?,
      unselectedLabelTextStyle:
          unselectedLabelTextStyle == const $CopyWithPlaceholder()
              ? _value.unselectedLabelTextStyle
              // ignore: cast_nullable_to_non_nullable
              : unselectedLabelTextStyle as ResizableTextStyle?,
      selectedLabelTextStyle:
          selectedLabelTextStyle == const $CopyWithPlaceholder()
              ? _value.selectedLabelTextStyle
              // ignore: cast_nullable_to_non_nullable
              : selectedLabelTextStyle as ResizableTextStyle?,
      unselectedIconTheme: unselectedIconTheme == const $CopyWithPlaceholder()
          ? _value.unselectedIconTheme
          // ignore: cast_nullable_to_non_nullable
          : unselectedIconTheme as IconThemeData?,
      selectedIconTheme: selectedIconTheme == const $CopyWithPlaceholder()
          ? _value.selectedIconTheme
          // ignore: cast_nullable_to_non_nullable
          : selectedIconTheme as IconThemeData?,
      useIndicator: useIndicator == const $CopyWithPlaceholder()
          ? _value.useIndicator
          // ignore: cast_nullable_to_non_nullable
          : useIndicator as bool?,
      indicatorColor: indicatorColor == const $CopyWithPlaceholder()
          ? _value.indicatorColor
          // ignore: cast_nullable_to_non_nullable
          : indicatorColor as Color?,
      indicatorShape: indicatorShape == const $CopyWithPlaceholder()
          ? _value.indicatorShape
          // ignore: cast_nullable_to_non_nullable
          : indicatorShape as ShapeBorder?,
      padding: padding == const $CopyWithPlaceholder() || padding == null
          ? _value.padding
          // ignore: cast_nullable_to_non_nullable
          : padding as EdgeInsetsGeometry,
    );
  }
}

extension $NavigationRailConfigCopyWith on NavigationRailConfig {
  /// Returns a callable class that can be used as follows: `instanceOfNavigationRailConfig.copyWith(...)` or like so:`instanceOfNavigationRailConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$NavigationRailConfigCWProxy get copyWith =>
      _$NavigationRailConfigCWProxyImpl(this);

  /// Copies the object with the specific fields set to `null`. If you pass `false` as a parameter, nothing will be done and it will be ignored. Don't do it. Prefer `copyWith(field: null)` or `NavigationRailConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// NavigationRailConfig(...).copyWithNull(firstField: true, secondField: true)
  /// ````
  NavigationRailConfig copyWithNull({
    bool selectedIndex = false,
    bool width = false,
    bool extendedWidth = false,
    bool leading = false,
    bool leadingExtended = false,
    bool trailing = false,
    bool key = false,
    bool backgroundColor = false,
    bool onDestinationSelected = false,
    bool elevation = false,
    bool groupAlignment = false,
    bool labelType = false,
    bool unselectedLabelTextStyle = false,
    bool selectedLabelTextStyle = false,
    bool unselectedIconTheme = false,
    bool selectedIconTheme = false,
    bool useIndicator = false,
    bool indicatorColor = false,
    bool indicatorShape = false,
  }) {
    return NavigationRailConfig(
      destinations: destinations,
      selectedIndex: selectedIndex == true ? null : this.selectedIndex,
      width: width == true ? null : this.width,
      extendedWidth: extendedWidth == true ? null : this.extendedWidth,
      leading: leading == true ? null : this.leading,
      leadingExtended: leadingExtended == true ? null : this.leadingExtended,
      trailing: trailing == true ? null : this.trailing,
      key: key == true ? null : this.key,
      backgroundColor: backgroundColor == true ? null : this.backgroundColor,
      onDestinationSelected:
          onDestinationSelected == true ? null : this.onDestinationSelected,
      elevation: elevation == true ? null : this.elevation,
      groupAlignment: groupAlignment == true ? null : this.groupAlignment,
      labelType: labelType == true ? null : this.labelType,
      unselectedLabelTextStyle: unselectedLabelTextStyle == true
          ? null
          : this.unselectedLabelTextStyle,
      selectedLabelTextStyle:
          selectedLabelTextStyle == true ? null : this.selectedLabelTextStyle,
      unselectedIconTheme:
          unselectedIconTheme == true ? null : this.unselectedIconTheme,
      selectedIconTheme:
          selectedIconTheme == true ? null : this.selectedIconTheme,
      useIndicator: useIndicator == true ? null : this.useIndicator,
      indicatorColor: indicatorColor == true ? null : this.indicatorColor,
      indicatorShape: indicatorShape == true ? null : this.indicatorShape,
      padding: padding,
    );
  }
}

abstract class _$BreakpointConfigCWProxy {
  BreakpointConfig large(Breakpoint large);

  BreakpointConfig medium(Breakpoint medium);

  BreakpointConfig small(Breakpoint small);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `BreakpointConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// BreakpointConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  BreakpointConfig call({
    Breakpoint? large,
    Breakpoint? medium,
    Breakpoint? small,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfBreakpointConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfBreakpointConfig.copyWith.fieldName(...)`
class _$BreakpointConfigCWProxyImpl implements _$BreakpointConfigCWProxy {
  const _$BreakpointConfigCWProxyImpl(this._value);

  final BreakpointConfig _value;

  @override
  BreakpointConfig large(Breakpoint large) => this(large: large);

  @override
  BreakpointConfig medium(Breakpoint medium) => this(medium: medium);

  @override
  BreakpointConfig small(Breakpoint small) => this(small: small);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `BreakpointConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// BreakpointConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  BreakpointConfig call({
    Object? large = const $CopyWithPlaceholder(),
    Object? medium = const $CopyWithPlaceholder(),
    Object? small = const $CopyWithPlaceholder(),
  }) {
    return BreakpointConfig(
      large: large == const $CopyWithPlaceholder() || large == null
          ? _value.large
          // ignore: cast_nullable_to_non_nullable
          : large as Breakpoint,
      medium: medium == const $CopyWithPlaceholder() || medium == null
          ? _value.medium
          // ignore: cast_nullable_to_non_nullable
          : medium as Breakpoint,
      small: small == const $CopyWithPlaceholder() || small == null
          ? _value.small
          // ignore: cast_nullable_to_non_nullable
          : small as Breakpoint,
    );
  }
}

extension $BreakpointConfigCopyWith on BreakpointConfig {
  /// Returns a callable class that can be used as follows: `instanceOfBreakpointConfig.copyWith(...)` or like so:`instanceOfBreakpointConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$BreakpointConfigCWProxy get copyWith => _$BreakpointConfigCWProxyImpl(this);
}

abstract class _$BodyConfigCWProxy {
  BodyConfig body(Widget Function(BuildContext)? body);

  BodyConfig large(Widget Function(BuildContext)? large);

  BodyConfig largeSecondary(Widget Function(BuildContext)? largeSecondary);

  BodyConfig orientation(Axis orientation);

  BodyConfig ratio(double? ratio);

  BodyConfig secondary(Widget Function(BuildContext)? secondary);

  BodyConfig small(Widget Function(BuildContext)? small);

  BodyConfig smallSecondary(Widget Function(BuildContext)? smallSecondary);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `BodyConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// BodyConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  BodyConfig call({
    Widget Function(BuildContext)? body,
    Widget Function(BuildContext)? large,
    Widget Function(BuildContext)? largeSecondary,
    Axis? orientation,
    double? ratio,
    Widget Function(BuildContext)? secondary,
    Widget Function(BuildContext)? small,
    Widget Function(BuildContext)? smallSecondary,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfBodyConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfBodyConfig.copyWith.fieldName(...)`
class _$BodyConfigCWProxyImpl implements _$BodyConfigCWProxy {
  const _$BodyConfigCWProxyImpl(this._value);

  final BodyConfig _value;

  @override
  BodyConfig body(Widget Function(BuildContext)? body) => this(body: body);

  @override
  BodyConfig large(Widget Function(BuildContext)? large) => this(large: large);

  @override
  BodyConfig largeSecondary(Widget Function(BuildContext)? largeSecondary) =>
      this(largeSecondary: largeSecondary);

  @override
  BodyConfig orientation(Axis orientation) => this(orientation: orientation);

  @override
  BodyConfig ratio(double? ratio) => this(ratio: ratio);

  @override
  BodyConfig secondary(Widget Function(BuildContext)? secondary) =>
      this(secondary: secondary);

  @override
  BodyConfig small(Widget Function(BuildContext)? small) => this(small: small);

  @override
  BodyConfig smallSecondary(Widget Function(BuildContext)? smallSecondary) =>
      this(smallSecondary: smallSecondary);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `BodyConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// BodyConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  BodyConfig call({
    Object? body = const $CopyWithPlaceholder(),
    Object? large = const $CopyWithPlaceholder(),
    Object? largeSecondary = const $CopyWithPlaceholder(),
    Object? orientation = const $CopyWithPlaceholder(),
    Object? ratio = const $CopyWithPlaceholder(),
    Object? secondary = const $CopyWithPlaceholder(),
    Object? small = const $CopyWithPlaceholder(),
    Object? smallSecondary = const $CopyWithPlaceholder(),
  }) {
    return BodyConfig(
      body: body == const $CopyWithPlaceholder()
          ? _value.body
          // ignore: cast_nullable_to_non_nullable
          : body as Widget Function(BuildContext)?,
      large: large == const $CopyWithPlaceholder()
          ? _value.large
          // ignore: cast_nullable_to_non_nullable
          : large as Widget Function(BuildContext)?,
      largeSecondary: largeSecondary == const $CopyWithPlaceholder()
          ? _value.largeSecondary
          // ignore: cast_nullable_to_non_nullable
          : largeSecondary as Widget Function(BuildContext)?,
      orientation:
          orientation == const $CopyWithPlaceholder() || orientation == null
              ? _value.orientation
              // ignore: cast_nullable_to_non_nullable
              : orientation as Axis,
      ratio: ratio == const $CopyWithPlaceholder()
          ? _value.ratio
          // ignore: cast_nullable_to_non_nullable
          : ratio as double?,
      secondary: secondary == const $CopyWithPlaceholder()
          ? _value.secondary
          // ignore: cast_nullable_to_non_nullable
          : secondary as Widget Function(BuildContext)?,
      small: small == const $CopyWithPlaceholder()
          ? _value.small
          // ignore: cast_nullable_to_non_nullable
          : small as Widget Function(BuildContext)?,
      smallSecondary: smallSecondary == const $CopyWithPlaceholder()
          ? _value.smallSecondary
          // ignore: cast_nullable_to_non_nullable
          : smallSecondary as Widget Function(BuildContext)?,
    );
  }
}

extension $BodyConfigCopyWith on BodyConfig {
  /// Returns a callable class that can be used as follows: `instanceOfBodyConfig.copyWith(...)` or like so:`instanceOfBodyConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$BodyConfigCWProxy get copyWith => _$BodyConfigCWProxyImpl(this);

  /// Copies the object with the specific fields set to `null`. If you pass `false` as a parameter, nothing will be done and it will be ignored. Don't do it. Prefer `copyWith(field: null)` or `BodyConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// BodyConfig(...).copyWithNull(firstField: true, secondField: true)
  /// ````
  BodyConfig copyWithNull({
    bool body = false,
    bool large = false,
    bool largeSecondary = false,
    bool ratio = false,
    bool secondary = false,
    bool small = false,
    bool smallSecondary = false,
  }) {
    return BodyConfig(
      body: body == true ? null : this.body,
      large: large == true ? null : this.large,
      largeSecondary: largeSecondary == true ? null : this.largeSecondary,
      orientation: orientation,
      ratio: ratio == true ? null : this.ratio,
      secondary: secondary == true ? null : this.secondary,
      small: small == true ? null : this.small,
      smallSecondary: smallSecondary == true ? null : this.smallSecondary,
    );
  }
}

abstract class _$AdaptiveDrawerConfigCWProxy {
  AdaptiveDrawerConfig key(Key? key);

  AdaptiveDrawerConfig children(List<Widget>? children);

  AdaptiveDrawerConfig backgroundColor(Color? backgroundColor);

  AdaptiveDrawerConfig shadowColor(Color? shadowColor);

  AdaptiveDrawerConfig surfaceTintColor(Color? surfaceTintColor);

  AdaptiveDrawerConfig elevation(double? elevation);

  AdaptiveDrawerConfig shape(ShapeBorder? shape);

  AdaptiveDrawerConfig width(double? width);

  AdaptiveDrawerConfig semanticLabel(String? semanticLabel);

  AdaptiveDrawerConfig indicatorColor(Color? indicatorColor);

  AdaptiveDrawerConfig indicatorShape(ShapeBorder? indicatorShape);

  AdaptiveDrawerConfig onDestinationSelected(
      void Function(int)? onDestinationSelected);

  AdaptiveDrawerConfig selectedIndex(int? selectedIndex);

  AdaptiveDrawerConfig breakpoint(Breakpoint breakpoint);

  AdaptiveDrawerConfig useDrawer(bool useDrawer);

  AdaptiveDrawerConfig useEndDrawer(bool useEndDrawer);

  AdaptiveDrawerConfig customDrawer(Widget? customDrawer);

  AdaptiveDrawerConfig onDrawerChanged(void Function(bool)? onDrawerChanged);

  AdaptiveDrawerConfig customEndDrawer(Widget? customEndDrawer);

  AdaptiveDrawerConfig onEndDrawerChanged(
      void Function(bool)? onEndDrawerChanged);

  AdaptiveDrawerConfig drawerDragStartBehavior(
      DragStartBehavior drawerDragStartBehavior);

  AdaptiveDrawerConfig drawerScrimColor(Color? drawerScrimColor);

  AdaptiveDrawerConfig drawerEdgeDragWidth(double? drawerEdgeDragWidth);

  AdaptiveDrawerConfig drawerEnableOpenDragGesture(
      bool drawerEnableOpenDragGesture);

  AdaptiveDrawerConfig endDrawerEnableOpenDragGesture(
      bool endDrawerEnableOpenDragGesture);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveDrawerConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveDrawerConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveDrawerConfig call({
    Key? key,
    List<Widget>? children,
    Color? backgroundColor,
    Color? shadowColor,
    Color? surfaceTintColor,
    double? elevation,
    ShapeBorder? shape,
    double? width,
    String? semanticLabel,
    Color? indicatorColor,
    ShapeBorder? indicatorShape,
    void Function(int)? onDestinationSelected,
    int? selectedIndex,
    Breakpoint? breakpoint,
    bool? useDrawer,
    bool? useEndDrawer,
    Widget? customDrawer,
    void Function(bool)? onDrawerChanged,
    Widget? customEndDrawer,
    void Function(bool)? onEndDrawerChanged,
    DragStartBehavior? drawerDragStartBehavior,
    Color? drawerScrimColor,
    double? drawerEdgeDragWidth,
    bool? drawerEnableOpenDragGesture,
    bool? endDrawerEnableOpenDragGesture,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfAdaptiveDrawerConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfAdaptiveDrawerConfig.copyWith.fieldName(...)`
class _$AdaptiveDrawerConfigCWProxyImpl
    implements _$AdaptiveDrawerConfigCWProxy {
  const _$AdaptiveDrawerConfigCWProxyImpl(this._value);

  final AdaptiveDrawerConfig _value;

  @override
  AdaptiveDrawerConfig key(Key? key) => this(key: key);

  @override
  AdaptiveDrawerConfig children(List<Widget>? children) =>
      this(children: children);

  @override
  AdaptiveDrawerConfig backgroundColor(Color? backgroundColor) =>
      this(backgroundColor: backgroundColor);

  @override
  AdaptiveDrawerConfig shadowColor(Color? shadowColor) =>
      this(shadowColor: shadowColor);

  @override
  AdaptiveDrawerConfig surfaceTintColor(Color? surfaceTintColor) =>
      this(surfaceTintColor: surfaceTintColor);

  @override
  AdaptiveDrawerConfig elevation(double? elevation) =>
      this(elevation: elevation);

  @override
  AdaptiveDrawerConfig shape(ShapeBorder? shape) => this(shape: shape);

  @override
  AdaptiveDrawerConfig width(double? width) => this(width: width);

  @override
  AdaptiveDrawerConfig semanticLabel(String? semanticLabel) =>
      this(semanticLabel: semanticLabel);

  @override
  AdaptiveDrawerConfig indicatorColor(Color? indicatorColor) =>
      this(indicatorColor: indicatorColor);

  @override
  AdaptiveDrawerConfig indicatorShape(ShapeBorder? indicatorShape) =>
      this(indicatorShape: indicatorShape);

  @override
  AdaptiveDrawerConfig onDestinationSelected(
          void Function(int)? onDestinationSelected) =>
      this(onDestinationSelected: onDestinationSelected);

  @override
  AdaptiveDrawerConfig selectedIndex(int? selectedIndex) =>
      this(selectedIndex: selectedIndex);

  @override
  AdaptiveDrawerConfig breakpoint(Breakpoint breakpoint) =>
      this(breakpoint: breakpoint);

  @override
  AdaptiveDrawerConfig useDrawer(bool useDrawer) => this(useDrawer: useDrawer);

  @override
  AdaptiveDrawerConfig useEndDrawer(bool useEndDrawer) =>
      this(useEndDrawer: useEndDrawer);

  @override
  AdaptiveDrawerConfig customDrawer(Widget? customDrawer) =>
      this(customDrawer: customDrawer);

  @override
  AdaptiveDrawerConfig onDrawerChanged(void Function(bool)? onDrawerChanged) =>
      this(onDrawerChanged: onDrawerChanged);

  @override
  AdaptiveDrawerConfig customEndDrawer(Widget? customEndDrawer) =>
      this(customEndDrawer: customEndDrawer);

  @override
  AdaptiveDrawerConfig onEndDrawerChanged(
          void Function(bool)? onEndDrawerChanged) =>
      this(onEndDrawerChanged: onEndDrawerChanged);

  @override
  AdaptiveDrawerConfig drawerDragStartBehavior(
          DragStartBehavior drawerDragStartBehavior) =>
      this(drawerDragStartBehavior: drawerDragStartBehavior);

  @override
  AdaptiveDrawerConfig drawerScrimColor(Color? drawerScrimColor) =>
      this(drawerScrimColor: drawerScrimColor);

  @override
  AdaptiveDrawerConfig drawerEdgeDragWidth(double? drawerEdgeDragWidth) =>
      this(drawerEdgeDragWidth: drawerEdgeDragWidth);

  @override
  AdaptiveDrawerConfig drawerEnableOpenDragGesture(
          bool drawerEnableOpenDragGesture) =>
      this(drawerEnableOpenDragGesture: drawerEnableOpenDragGesture);

  @override
  AdaptiveDrawerConfig endDrawerEnableOpenDragGesture(
          bool endDrawerEnableOpenDragGesture) =>
      this(endDrawerEnableOpenDragGesture: endDrawerEnableOpenDragGesture);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveDrawerConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveDrawerConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveDrawerConfig call({
    Object? key = const $CopyWithPlaceholder(),
    Object? children = const $CopyWithPlaceholder(),
    Object? backgroundColor = const $CopyWithPlaceholder(),
    Object? shadowColor = const $CopyWithPlaceholder(),
    Object? surfaceTintColor = const $CopyWithPlaceholder(),
    Object? elevation = const $CopyWithPlaceholder(),
    Object? shape = const $CopyWithPlaceholder(),
    Object? width = const $CopyWithPlaceholder(),
    Object? semanticLabel = const $CopyWithPlaceholder(),
    Object? indicatorColor = const $CopyWithPlaceholder(),
    Object? indicatorShape = const $CopyWithPlaceholder(),
    Object? onDestinationSelected = const $CopyWithPlaceholder(),
    Object? selectedIndex = const $CopyWithPlaceholder(),
    Object? breakpoint = const $CopyWithPlaceholder(),
    Object? useDrawer = const $CopyWithPlaceholder(),
    Object? useEndDrawer = const $CopyWithPlaceholder(),
    Object? customDrawer = const $CopyWithPlaceholder(),
    Object? onDrawerChanged = const $CopyWithPlaceholder(),
    Object? customEndDrawer = const $CopyWithPlaceholder(),
    Object? onEndDrawerChanged = const $CopyWithPlaceholder(),
    Object? drawerDragStartBehavior = const $CopyWithPlaceholder(),
    Object? drawerScrimColor = const $CopyWithPlaceholder(),
    Object? drawerEdgeDragWidth = const $CopyWithPlaceholder(),
    Object? drawerEnableOpenDragGesture = const $CopyWithPlaceholder(),
    Object? endDrawerEnableOpenDragGesture = const $CopyWithPlaceholder(),
  }) {
    return AdaptiveDrawerConfig(
      key: key == const $CopyWithPlaceholder()
          ? _value.key
          // ignore: cast_nullable_to_non_nullable
          : key as Key?,
      children: children == const $CopyWithPlaceholder()
          ? _value.children
          // ignore: cast_nullable_to_non_nullable
          : children as List<Widget>?,
      backgroundColor: backgroundColor == const $CopyWithPlaceholder()
          ? _value.backgroundColor
          // ignore: cast_nullable_to_non_nullable
          : backgroundColor as Color?,
      shadowColor: shadowColor == const $CopyWithPlaceholder()
          ? _value.shadowColor
          // ignore: cast_nullable_to_non_nullable
          : shadowColor as Color?,
      surfaceTintColor: surfaceTintColor == const $CopyWithPlaceholder()
          ? _value.surfaceTintColor
          // ignore: cast_nullable_to_non_nullable
          : surfaceTintColor as Color?,
      elevation: elevation == const $CopyWithPlaceholder()
          ? _value.elevation
          // ignore: cast_nullable_to_non_nullable
          : elevation as double?,
      shape: shape == const $CopyWithPlaceholder()
          ? _value.shape
          // ignore: cast_nullable_to_non_nullable
          : shape as ShapeBorder?,
      width: width == const $CopyWithPlaceholder()
          ? _value.width
          // ignore: cast_nullable_to_non_nullable
          : width as double?,
      semanticLabel: semanticLabel == const $CopyWithPlaceholder()
          ? _value.semanticLabel
          // ignore: cast_nullable_to_non_nullable
          : semanticLabel as String?,
      indicatorColor: indicatorColor == const $CopyWithPlaceholder()
          ? _value.indicatorColor
          // ignore: cast_nullable_to_non_nullable
          : indicatorColor as Color?,
      indicatorShape: indicatorShape == const $CopyWithPlaceholder()
          ? _value.indicatorShape
          // ignore: cast_nullable_to_non_nullable
          : indicatorShape as ShapeBorder?,
      onDestinationSelected:
          onDestinationSelected == const $CopyWithPlaceholder()
              ? _value.onDestinationSelected
              // ignore: cast_nullable_to_non_nullable
              : onDestinationSelected as void Function(int)?,
      selectedIndex: selectedIndex == const $CopyWithPlaceholder()
          ? _value.selectedIndex
          // ignore: cast_nullable_to_non_nullable
          : selectedIndex as int?,
      breakpoint:
          breakpoint == const $CopyWithPlaceholder() || breakpoint == null
              ? _value.breakpoint
              // ignore: cast_nullable_to_non_nullable
              : breakpoint as Breakpoint,
      useDrawer: useDrawer == const $CopyWithPlaceholder() || useDrawer == null
          ? _value.useDrawer
          // ignore: cast_nullable_to_non_nullable
          : useDrawer as bool,
      useEndDrawer:
          useEndDrawer == const $CopyWithPlaceholder() || useEndDrawer == null
              ? _value.useEndDrawer
              // ignore: cast_nullable_to_non_nullable
              : useEndDrawer as bool,
      customDrawer: customDrawer == const $CopyWithPlaceholder()
          ? _value.customDrawer
          // ignore: cast_nullable_to_non_nullable
          : customDrawer as Widget?,
      onDrawerChanged: onDrawerChanged == const $CopyWithPlaceholder()
          ? _value.onDrawerChanged
          // ignore: cast_nullable_to_non_nullable
          : onDrawerChanged as void Function(bool)?,
      customEndDrawer: customEndDrawer == const $CopyWithPlaceholder()
          ? _value.customEndDrawer
          // ignore: cast_nullable_to_non_nullable
          : customEndDrawer as Widget?,
      onEndDrawerChanged: onEndDrawerChanged == const $CopyWithPlaceholder()
          ? _value.onEndDrawerChanged
          // ignore: cast_nullable_to_non_nullable
          : onEndDrawerChanged as void Function(bool)?,
      drawerDragStartBehavior:
          drawerDragStartBehavior == const $CopyWithPlaceholder() ||
                  drawerDragStartBehavior == null
              ? _value.drawerDragStartBehavior
              // ignore: cast_nullable_to_non_nullable
              : drawerDragStartBehavior as DragStartBehavior,
      drawerScrimColor: drawerScrimColor == const $CopyWithPlaceholder()
          ? _value.drawerScrimColor
          // ignore: cast_nullable_to_non_nullable
          : drawerScrimColor as Color?,
      drawerEdgeDragWidth: drawerEdgeDragWidth == const $CopyWithPlaceholder()
          ? _value.drawerEdgeDragWidth
          // ignore: cast_nullable_to_non_nullable
          : drawerEdgeDragWidth as double?,
      drawerEnableOpenDragGesture:
          drawerEnableOpenDragGesture == const $CopyWithPlaceholder() ||
                  drawerEnableOpenDragGesture == null
              ? _value.drawerEnableOpenDragGesture
              // ignore: cast_nullable_to_non_nullable
              : drawerEnableOpenDragGesture as bool,
      endDrawerEnableOpenDragGesture:
          endDrawerEnableOpenDragGesture == const $CopyWithPlaceholder() ||
                  endDrawerEnableOpenDragGesture == null
              ? _value.endDrawerEnableOpenDragGesture
              // ignore: cast_nullable_to_non_nullable
              : endDrawerEnableOpenDragGesture as bool,
    );
  }
}

extension $AdaptiveDrawerConfigCopyWith on AdaptiveDrawerConfig {
  /// Returns a callable class that can be used as follows: `instanceOfAdaptiveDrawerConfig.copyWith(...)` or like so:`instanceOfAdaptiveDrawerConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$AdaptiveDrawerConfigCWProxy get copyWith =>
      _$AdaptiveDrawerConfigCWProxyImpl(this);

  /// Copies the object with the specific fields set to `null`. If you pass `false` as a parameter, nothing will be done and it will be ignored. Don't do it. Prefer `copyWith(field: null)` or `AdaptiveDrawerConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveDrawerConfig(...).copyWithNull(firstField: true, secondField: true)
  /// ````
  AdaptiveDrawerConfig copyWithNull({
    bool key = false,
    bool children = false,
    bool backgroundColor = false,
    bool shadowColor = false,
    bool surfaceTintColor = false,
    bool elevation = false,
    bool shape = false,
    bool width = false,
    bool semanticLabel = false,
    bool indicatorColor = false,
    bool indicatorShape = false,
    bool onDestinationSelected = false,
    bool selectedIndex = false,
    bool customDrawer = false,
    bool onDrawerChanged = false,
    bool customEndDrawer = false,
    bool onEndDrawerChanged = false,
    bool drawerScrimColor = false,
    bool drawerEdgeDragWidth = false,
  }) {
    return AdaptiveDrawerConfig(
      key: key == true ? null : this.key,
      children: children == true ? null : this.children,
      backgroundColor: backgroundColor == true ? null : this.backgroundColor,
      shadowColor: shadowColor == true ? null : this.shadowColor,
      surfaceTintColor: surfaceTintColor == true ? null : this.surfaceTintColor,
      elevation: elevation == true ? null : this.elevation,
      shape: shape == true ? null : this.shape,
      width: width == true ? null : this.width,
      semanticLabel: semanticLabel == true ? null : this.semanticLabel,
      indicatorColor: indicatorColor == true ? null : this.indicatorColor,
      indicatorShape: indicatorShape == true ? null : this.indicatorShape,
      onDestinationSelected:
          onDestinationSelected == true ? null : this.onDestinationSelected,
      selectedIndex: selectedIndex == true ? null : this.selectedIndex,
      breakpoint: breakpoint,
      useDrawer: useDrawer,
      useEndDrawer: useEndDrawer,
      customDrawer: customDrawer == true ? null : this.customDrawer,
      onDrawerChanged: onDrawerChanged == true ? null : this.onDrawerChanged,
      customEndDrawer: customEndDrawer == true ? null : this.customEndDrawer,
      onEndDrawerChanged:
          onEndDrawerChanged == true ? null : this.onEndDrawerChanged,
      drawerDragStartBehavior: drawerDragStartBehavior,
      drawerScrimColor: drawerScrimColor == true ? null : this.drawerScrimColor,
      drawerEdgeDragWidth:
          drawerEdgeDragWidth == true ? null : this.drawerEdgeDragWidth,
      drawerEnableOpenDragGesture: drawerEnableOpenDragGesture,
      endDrawerEnableOpenDragGesture: endDrawerEnableOpenDragGesture,
    );
  }
}

abstract class _$AdaptiveScrollbarConfigCWProxy {
  AdaptiveScrollbarConfig controller(ScrollController? controller);

  AdaptiveScrollbarConfig width(double width);

  AdaptiveScrollbarConfig sliderHeight(double? sliderHeight);

  AdaptiveScrollbarConfig sliderChild(Widget? sliderChild);

  AdaptiveScrollbarConfig sliderDefaultColor(Color sliderDefaultColor);

  AdaptiveScrollbarConfig sliderActiveColor(Color? sliderActiveColor);

  AdaptiveScrollbarConfig underColor(Color underColor);

  AdaptiveScrollbarConfig underSpacing(EdgeInsetsGeometry underSpacing);

  AdaptiveScrollbarConfig sliderSpacing(EdgeInsetsGeometry sliderSpacing);

  AdaptiveScrollbarConfig scrollToClickDelta(double scrollToClickDelta);

  AdaptiveScrollbarConfig scrollToClickFirstDelay(int scrollToClickFirstDelay);

  AdaptiveScrollbarConfig scrollToClickOtherDelay(int scrollToClickOtherDelay);

  AdaptiveScrollbarConfig underDecoration(BoxDecoration? underDecoration);

  AdaptiveScrollbarConfig sliderDecoration(BoxDecoration? sliderDecoration);

  AdaptiveScrollbarConfig sliderActiveDecoration(
      BoxDecoration? sliderActiveDecoration);

  AdaptiveScrollbarConfig key(Key? key);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveScrollbarConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScrollbarConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveScrollbarConfig call({
    ScrollController? controller,
    double? width,
    double? sliderHeight,
    Widget? sliderChild,
    Color? sliderDefaultColor,
    Color? sliderActiveColor,
    Color? underColor,
    EdgeInsetsGeometry? underSpacing,
    EdgeInsetsGeometry? sliderSpacing,
    double? scrollToClickDelta,
    int? scrollToClickFirstDelay,
    int? scrollToClickOtherDelay,
    BoxDecoration? underDecoration,
    BoxDecoration? sliderDecoration,
    BoxDecoration? sliderActiveDecoration,
    Key? key,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfAdaptiveScrollbarConfig.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfAdaptiveScrollbarConfig.copyWith.fieldName(...)`
class _$AdaptiveScrollbarConfigCWProxyImpl
    implements _$AdaptiveScrollbarConfigCWProxy {
  const _$AdaptiveScrollbarConfigCWProxyImpl(this._value);

  final AdaptiveScrollbarConfig _value;

  @override
  AdaptiveScrollbarConfig controller(ScrollController? controller) =>
      this(controller: controller);

  @override
  AdaptiveScrollbarConfig width(double width) => this(width: width);

  @override
  AdaptiveScrollbarConfig sliderHeight(double? sliderHeight) =>
      this(sliderHeight: sliderHeight);

  @override
  AdaptiveScrollbarConfig sliderChild(Widget? sliderChild) =>
      this(sliderChild: sliderChild);

  @override
  AdaptiveScrollbarConfig sliderDefaultColor(Color sliderDefaultColor) =>
      this(sliderDefaultColor: sliderDefaultColor);

  @override
  AdaptiveScrollbarConfig sliderActiveColor(Color? sliderActiveColor) =>
      this(sliderActiveColor: sliderActiveColor);

  @override
  AdaptiveScrollbarConfig underColor(Color underColor) =>
      this(underColor: underColor);

  @override
  AdaptiveScrollbarConfig underSpacing(EdgeInsetsGeometry underSpacing) =>
      this(underSpacing: underSpacing);

  @override
  AdaptiveScrollbarConfig sliderSpacing(EdgeInsetsGeometry sliderSpacing) =>
      this(sliderSpacing: sliderSpacing);

  @override
  AdaptiveScrollbarConfig scrollToClickDelta(double scrollToClickDelta) =>
      this(scrollToClickDelta: scrollToClickDelta);

  @override
  AdaptiveScrollbarConfig scrollToClickFirstDelay(
          int scrollToClickFirstDelay) =>
      this(scrollToClickFirstDelay: scrollToClickFirstDelay);

  @override
  AdaptiveScrollbarConfig scrollToClickOtherDelay(
          int scrollToClickOtherDelay) =>
      this(scrollToClickOtherDelay: scrollToClickOtherDelay);

  @override
  AdaptiveScrollbarConfig underDecoration(BoxDecoration? underDecoration) =>
      this(underDecoration: underDecoration);

  @override
  AdaptiveScrollbarConfig sliderDecoration(BoxDecoration? sliderDecoration) =>
      this(sliderDecoration: sliderDecoration);

  @override
  AdaptiveScrollbarConfig sliderActiveDecoration(
          BoxDecoration? sliderActiveDecoration) =>
      this(sliderActiveDecoration: sliderActiveDecoration);

  @override
  AdaptiveScrollbarConfig key(Key? key) => this(key: key);

  @override

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `AdaptiveScrollbarConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScrollbarConfig(...).copyWith(id: 12, name: "My name")
  /// ````
  AdaptiveScrollbarConfig call({
    Object? controller = const $CopyWithPlaceholder(),
    Object? width = const $CopyWithPlaceholder(),
    Object? sliderHeight = const $CopyWithPlaceholder(),
    Object? sliderChild = const $CopyWithPlaceholder(),
    Object? sliderDefaultColor = const $CopyWithPlaceholder(),
    Object? sliderActiveColor = const $CopyWithPlaceholder(),
    Object? underColor = const $CopyWithPlaceholder(),
    Object? underSpacing = const $CopyWithPlaceholder(),
    Object? sliderSpacing = const $CopyWithPlaceholder(),
    Object? scrollToClickDelta = const $CopyWithPlaceholder(),
    Object? scrollToClickFirstDelay = const $CopyWithPlaceholder(),
    Object? scrollToClickOtherDelay = const $CopyWithPlaceholder(),
    Object? underDecoration = const $CopyWithPlaceholder(),
    Object? sliderDecoration = const $CopyWithPlaceholder(),
    Object? sliderActiveDecoration = const $CopyWithPlaceholder(),
    Object? key = const $CopyWithPlaceholder(),
  }) {
    return AdaptiveScrollbarConfig(
      controller: controller == const $CopyWithPlaceholder()
          ? _value.controller
          // ignore: cast_nullable_to_non_nullable
          : controller as ScrollController?,
      width: width == const $CopyWithPlaceholder() || width == null
          ? _value.width
          // ignore: cast_nullable_to_non_nullable
          : width as double,
      sliderHeight: sliderHeight == const $CopyWithPlaceholder()
          ? _value.sliderHeight
          // ignore: cast_nullable_to_non_nullable
          : sliderHeight as double?,
      sliderChild: sliderChild == const $CopyWithPlaceholder()
          ? _value.sliderChild
          // ignore: cast_nullable_to_non_nullable
          : sliderChild as Widget?,
      sliderDefaultColor: sliderDefaultColor == const $CopyWithPlaceholder() ||
              sliderDefaultColor == null
          ? _value.sliderDefaultColor
          // ignore: cast_nullable_to_non_nullable
          : sliderDefaultColor as Color,
      sliderActiveColor: sliderActiveColor == const $CopyWithPlaceholder()
          ? _value.sliderActiveColor
          // ignore: cast_nullable_to_non_nullable
          : sliderActiveColor as Color?,
      underColor:
          underColor == const $CopyWithPlaceholder() || underColor == null
              ? _value.underColor
              // ignore: cast_nullable_to_non_nullable
              : underColor as Color,
      underSpacing:
          underSpacing == const $CopyWithPlaceholder() || underSpacing == null
              ? _value.underSpacing
              // ignore: cast_nullable_to_non_nullable
              : underSpacing as EdgeInsetsGeometry,
      sliderSpacing:
          sliderSpacing == const $CopyWithPlaceholder() || sliderSpacing == null
              ? _value.sliderSpacing
              // ignore: cast_nullable_to_non_nullable
              : sliderSpacing as EdgeInsetsGeometry,
      scrollToClickDelta: scrollToClickDelta == const $CopyWithPlaceholder() ||
              scrollToClickDelta == null
          ? _value.scrollToClickDelta
          // ignore: cast_nullable_to_non_nullable
          : scrollToClickDelta as double,
      scrollToClickFirstDelay:
          scrollToClickFirstDelay == const $CopyWithPlaceholder() ||
                  scrollToClickFirstDelay == null
              ? _value.scrollToClickFirstDelay
              // ignore: cast_nullable_to_non_nullable
              : scrollToClickFirstDelay as int,
      scrollToClickOtherDelay:
          scrollToClickOtherDelay == const $CopyWithPlaceholder() ||
                  scrollToClickOtherDelay == null
              ? _value.scrollToClickOtherDelay
              // ignore: cast_nullable_to_non_nullable
              : scrollToClickOtherDelay as int,
      underDecoration: underDecoration == const $CopyWithPlaceholder()
          ? _value.underDecoration
          // ignore: cast_nullable_to_non_nullable
          : underDecoration as BoxDecoration?,
      sliderDecoration: sliderDecoration == const $CopyWithPlaceholder()
          ? _value.sliderDecoration
          // ignore: cast_nullable_to_non_nullable
          : sliderDecoration as BoxDecoration?,
      sliderActiveDecoration:
          sliderActiveDecoration == const $CopyWithPlaceholder()
              ? _value.sliderActiveDecoration
              // ignore: cast_nullable_to_non_nullable
              : sliderActiveDecoration as BoxDecoration?,
      key: key == const $CopyWithPlaceholder()
          ? _value.key
          // ignore: cast_nullable_to_non_nullable
          : key as Key?,
    );
  }
}

extension $AdaptiveScrollbarConfigCopyWith on AdaptiveScrollbarConfig {
  /// Returns a callable class that can be used as follows: `instanceOfAdaptiveScrollbarConfig.copyWith(...)` or like so:`instanceOfAdaptiveScrollbarConfig.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$AdaptiveScrollbarConfigCWProxy get copyWith =>
      _$AdaptiveScrollbarConfigCWProxyImpl(this);

  /// Copies the object with the specific fields set to `null`. If you pass `false` as a parameter, nothing will be done and it will be ignored. Don't do it. Prefer `copyWith(field: null)` or `AdaptiveScrollbarConfig(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// AdaptiveScrollbarConfig(...).copyWithNull(firstField: true, secondField: true)
  /// ````
  AdaptiveScrollbarConfig copyWithNull({
    bool controller = false,
    bool sliderHeight = false,
    bool sliderChild = false,
    bool sliderActiveColor = false,
    bool underDecoration = false,
    bool sliderDecoration = false,
    bool sliderActiveDecoration = false,
    bool key = false,
  }) {
    return AdaptiveScrollbarConfig(
      controller: controller == true ? null : this.controller,
      width: width,
      sliderHeight: sliderHeight == true ? null : this.sliderHeight,
      sliderChild: sliderChild == true ? null : this.sliderChild,
      sliderDefaultColor: sliderDefaultColor,
      sliderActiveColor:
          sliderActiveColor == true ? null : this.sliderActiveColor,
      underColor: underColor,
      underSpacing: underSpacing,
      sliderSpacing: sliderSpacing,
      scrollToClickDelta: scrollToClickDelta,
      scrollToClickFirstDelay: scrollToClickFirstDelay,
      scrollToClickOtherDelay: scrollToClickOtherDelay,
      underDecoration: underDecoration == true ? null : this.underDecoration,
      sliderDecoration: sliderDecoration == true ? null : this.sliderDecoration,
      sliderActiveDecoration:
          sliderActiveDecoration == true ? null : this.sliderActiveDecoration,
      key: key == true ? null : this.key,
    );
  }
}
